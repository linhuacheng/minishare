// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.sjsu.minishare.web;

import com.sjsu.minishare.model.CloudUser;
import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.String;
import java.util.Collection;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect RegistrationController_Roo_Controller {
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String RegistrationController.createForm(Model uiModel) {
        uiModel.addAttribute("cloudUser", new CloudUser());
        return "registration/create";
    }
    
    @RequestMapping(value = "/{userId}", method = RequestMethod.GET)
    public String RegistrationController.show(@PathVariable("userId") Integer userId, Model uiModel) {
        uiModel.addAttribute("clouduser", CloudUser.findCloudUser(userId));
        uiModel.addAttribute("itemId", userId);
        return "registration/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String RegistrationController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            uiModel.addAttribute("cloudusers", CloudUser.findCloudUserEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) CloudUser.countCloudUsers() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("cloudusers", CloudUser.findAllCloudUsers());
        }
        return "registration/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String RegistrationController.update(@Valid CloudUser cloudUser, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("cloudUser", cloudUser);
            return "registration/update";
        }
        uiModel.asMap().clear();
        cloudUser.merge();
        return "redirect:/registration/" + encodeUrlPathSegment(cloudUser.getUserId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{userId}", params = "form", method = RequestMethod.GET)
    public String RegistrationController.updateForm(@PathVariable("userId") Integer userId, Model uiModel) {
        uiModel.addAttribute("cloudUser", CloudUser.findCloudUser(userId));
        return "registration/update";
    }
    
    @RequestMapping(value = "/{userId}", method = RequestMethod.DELETE)
    public String RegistrationController.delete(@PathVariable("userId") Integer userId, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        CloudUser.findCloudUser(userId).remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/registration";
    }
    
    @ModelAttribute("cloudusers")
    public Collection<CloudUser> RegistrationController.populateCloudUsers() {
        return CloudUser.findAllCloudUsers();
    }
    
    String RegistrationController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
